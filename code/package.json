{
  "name": "@puresoc/workspace",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.33.2",
  "scripts": {
    "lint": "node scripts/check-layout.mjs && tsc -p tsconfig.base.json --noEmit",
    "typecheck": "tsc -p tsconfig.base.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "compose:config": "docker compose -f infra/compose/docker-compose.yml config"
  },
  "devDependencies": {
    "@types/node": "^22.10.10",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
