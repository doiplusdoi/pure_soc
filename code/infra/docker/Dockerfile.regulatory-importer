FROM node:22-alpine
WORKDIR /workspace
COPY package.json pnpm-workspace.yaml tsconfig.base.json ./
COPY apps/regulatory-importer ./apps/regulatory-importer
COPY packages ./packages
COPY data ./data
CMD ["node", "-e", "console.log('puresoc-regulatory-importer ready')"]
