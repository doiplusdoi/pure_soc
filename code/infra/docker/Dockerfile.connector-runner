FROM node:22-alpine
WORKDIR /workspace
COPY package.json pnpm-workspace.yaml tsconfig.base.json ./
COPY apps/connector-runner ./apps/connector-runner
COPY packages ./packages
CMD ["node", "-e", "setInterval(() => {}, 1000)"]
